# Checkout model usage

- All QA/design/i18n/review subagents this session run on `hpc-vllm/Qwen3.8-27B` (TalTech-provided Qwen3.8 27B), per the owner's model policy.
- The main orchestrator (this chat) runs on the session model.
- No alternate model used for implementation; web research (de-AI design) used the configured web_search provider.
- Fallback events: none so far.

Subagent IDs are recorded in checkout-session-status.md.
